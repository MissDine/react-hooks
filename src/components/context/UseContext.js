import React from 'react'
// This is all about passing properties from the parent component down to the child components
// Context provides a way of passing data through the component tree without having to pass the props down manually.

function UseContext() {
  return (
    <div>UseContext</div>
  )
}

export default UseContext